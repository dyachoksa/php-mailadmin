{% from "_forms" import text_field, password_field, list_field, boolean_field, action_buttons %}

{{ form_hidden('active', 0) }}

{% set domain_title = trans('mailadmin.domain') %}
{{ list_field('domain_id', domain_title, domains_list) }}

{{ text_field('email', 'Email') }}

{% set password_label = trans('mailadmin.password') %}
{{ password_field('password', password_label) }}

{% set active_label = trans('mailadmin.active') %}
{{ boolean_field('active', active_label) }}

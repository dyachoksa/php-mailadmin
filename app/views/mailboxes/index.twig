{% extends "layout" %}

{% import "_helpers" as helpers %}

{% block title %}{{ trans('mailadmin.mailboxes') }}{% endblock %}

{% block content %}
  <div class="page-header">
    <h1>
      {{ trans('mailadmin.mailboxes') }}
      <a href="{{ route('mailboxes.create') }}" class="btn btn-success pull-right"
         title="{{ trans('mailadmin.new_mailbox_title') }}">{{ trans('mailadmin.add') }}</a></h1>
  </div>

  {% include "mailboxes/_list" %}

  {{ mailboxes.links | raw }}
{% endblock %}

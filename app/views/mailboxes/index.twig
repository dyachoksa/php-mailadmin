{% extends "layout" %}

{% import "_helpers" as helpers %}

{% block title %}Mailboxes{% endblock %}

{% block content %}
  <div class="page-header">
    <h1>Mailboxes <a href="{{ route('mailboxes.create') }}" class="btn btn-success pull-right" title="Add a new mailbox">
        Add new</a></h1>
  </div>

  {% include "mailboxes/_list" %}

  {{ mailboxes.links | raw }}
{% endblock %}

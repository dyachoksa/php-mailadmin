{% from "_forms" import text_field, password_field, list_field, boolean_field, action_buttons %}

{{ form_hidden('active', 0) }}

{% set domain_label = trans('mailadmin.domain') %}
{{ list_field('domain_id', domain_label, domains_list) }}

{% set source_label = trans('mailadmin.source') %}
{{ text_field('source', source_label) }}

{% set destination_label = trans('mailadmin.destination') %}
{{ text_field('destination', destination_label) }}

{% set active_label = trans('mailadmin.active') %}
{{ boolean_field('active', active_label) }}

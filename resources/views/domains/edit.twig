{% extends 'base' %}

{% block page_title %}{{ trans('pages.domains.edit.title') }}{% endblock %}

{% block title %}{{ trans('pages.domains.edit.title') }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-offset-2 col-md-8">
            <div class="box">
                <div class="box-header"></div>

                {{ form_model(domain, {'route': ['domains.update', domain.id], 'method': 'PUT', 'class': 'form-horizontal'}) }}
                    {% include 'domains.form' %}
                {{ form_close() }}
            </div>
        </div>
    </div>
{% endblock %}
